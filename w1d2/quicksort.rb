def quicksort(arr, pivot = 0)
  return arr if pivot == arr.length

  #partition puts one item in its correct position
  #partition
  #check for values less than partion

end
def swap(arr, i, j)
  arr[i], arr[j] = arr[j], arr[i]
end

arr = ['k', 'r', 'a', 't', 'e', 'l', 'e', 'p', 'u',
      'i', 'm', 'q', 'c', 'x', 'o', 's']
# p quicksort(arr)
